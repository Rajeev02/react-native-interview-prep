# S21 — Server-Driven UI

> JSON schemas · experimentation · component registries · plugin/widget · sandboxing · backward compat

> Status: **scaffold** — fill with content using the per-topic format from `final-prompt.md`.

## Topics

- JSON-driven UI schemas
- Experimentation & A/B systems
- Remote layouts & component registries
- Plugin / widget architecture
- Safety, validation (Zod), and sandboxing
- Backward compatibility for SDUI
- Versioning + capability negotiation
- Caching strategies (preloaded layouts, fallback bundles)

## Q-topics (stubs)

- Q1. SDUI schema design — components, props, slots
- Q2. Validation + safety boundary (Zod, schema versioning)
- Q3. Backward compatibility — capability flags, fallback components
- Q4. SDUI + experimentation (A/B, holdouts)
- Q5. Performance — preloading, caching, fallback bundles

> Cross-refs: S16 (architecture), S22 (system design), S8 (state).
