# S5 — Expo & Tooling

> Expo SDK · Modules API · Expo Router · Config Plugins · EAS Build/Update/Submit · CNG · OTA

> Status: **scaffold** — fill with content using the per-topic format from `final-prompt.md`.

## Topics

- Expo SDK lifecycle (release cadence, RN compatibility matrix)
- Expo Modules API (Swift / Kotlin) for writing native modules
- Expo Router (file-based routing, layouts, typed routes, lazy loading)
- Config Plugins (mods, dangerous mods, withDangerousMod)
- EAS Build (profiles, credentials, custom workflows, build hooks)
- EAS Update (channels, branches, runtime versions, rollback)
- EAS Submit (TestFlight / Play Console internal track)
- Continuous Native Generation (CNG) & prebuild
- Development builds vs Expo Go (when each makes sense)
- OTA strategy & rollback
- Expo vs bare workflow tradeoffs
- Expo Modules API vs TurboModules

## Q-topics (stubs)

- Q1. Expo Router — file-based routing, layouts, typed routes
- Q2. Config Plugins — when, why, dangerous mods
- Q3. EAS Build profiles + credentials at scale
- Q4. EAS Update — channels, branches, runtime versions, rollback
- Q5. CNG / prebuild and brownfield integration
- Q6. Expo Modules API vs writing a TurboModule

> Cross-refs: `appendix/J-expo-router-config-plugins.md`, S20 (CI/CD).
